# APScheduler 기반 백그라운드 잡을 본 모듈에 정의한다.
# 외부 예보 API를 cron 주기로 폴링하여 캐시를 사전 적재한다.


def kma_polling_job() -> None:
    """기상청 단기·중기 예보를 폴링하여 캐시에 적재하는 잡.

    발표 시각 + 일정 지연 후 cron으로 실행되며, 사전 등록된 좌표 집합에
    대해서만 호출하여 외부 API 사용량을 최소화한다.
    """
    pass
